[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ferric"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "maturin>=1.13.3",
    "numpy>=2.2.6",
]

[tool.maturin]
features = ["pyo3/extension-module"]
