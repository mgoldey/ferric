libint*
libferric*
libxc*
!**/src/libxc.rs
!**/tests/libxc_smoke.rs
build
target
.venv/
*.key
*.pdf
diss*.txt
dissertation.txt
test_minimax*.py
main.py
cc.sh
alkane_C10.xyz
examples/dft_pbe.py

# Claude session state and worktrees
.claude/

# Compute outputs in repo root — should not be tracked
/*.cube
/*.npz
/benzene_*.cube
/benzene_*.npz

# Deprecated code bucket
/deprecated/

# Local handoff notes and presentation drafts (not engine code)
/handoff.txt
/.handoff-task50.txt
/demo-talk/


# grid run artifacts (regenerated; out/ is large + actively written)
benchmarks/grid/out/
benchmarks/grid/geoms/
benchmarks/grid/toml/
benchmarks/grid/*.log
benchmarks/grid/status.json
benchmarks/gmtkn30/__pycache__/
benchmarks/grid/__pycache__/
# latex build products
docs/papers/*.aux
docs/papers/*.fdb_latexmk
docs/papers/*.fls
docs/papers/*.log

# Python bytecode — everywhere
__pycache__/
*.pyc

# Scratch zone: load-gated queue stdout, one-off probes, debug logs.
# Promote a file into the permanent record deliberately with `git add -f`.
scripts/queue/out/
scripts/**/*.log
